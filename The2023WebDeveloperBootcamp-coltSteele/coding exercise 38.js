// DEFINE YOUR FUNCTION BELOW:
function multiply(firstNumber, secondNumber) {
    return firstNumber * secondNumber;
}

multiply(2, 3);
multiply(9, 9);
multiply(5, 4);
// should log to the console if the result has to be printed and viewed by the user
